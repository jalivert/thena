thena spine> data Nat : Type₀ where { zero : Nat ; succ : Nat -> Nat }
declared Nat
thena spine> data Eq {l} (A : Type {l}) : A -> A -> Type {l} where { refl {0} : ∀ (a : A) -> Eq {0} A a a }
1:57: unexpected {
thena spine> :theorem id0 : ∀ (A : Type₀) -> A -> A
proving id0 : ∀ (A : Type₀) -> A -> A
thena spine> unify-refine (\ (A : Type₀) (a : A) -> a)
already equal
thena spine> :show
  let refined = λ (A : Type₀) (a : A) -> a : ∀ (A : Type₀) -> A -> A in
▶ let id0 = refined : ∀ (A : Type₀) -> A -> A in
  id0
thena spine> qed
id0 : ∀ (A : Type₀) -> A -> A   ∎
thena spine> :theorem refl0 : Eq {0} Nat zero zero
not in scope: Eq
thena spine> claim A : Type₀
claimed A
thena spine> claim a : A
claimed a
thena spine> unify-refine (refl {0} A a)
not in scope: refl
thena spine> :show
  let ? A : Type₀ in
  let ? a : A in
▶ let ? goal : Type₀ in
  goal
thena spine> qed
no proof is being worked on
thena spine> :show refl0
nothing named refl0 has been declared
thena spine> :theorem wrong : Eq {0} Nat zero (succ zero)
not in scope: Eq
thena spine> unify-refine (refl {0} Nat zero)
not in scope: refl
thena spine> :show
  let ? A : Type₀ in
  let ? a : A in
▶ let ? goal : Type₀ in
  goal
thena spine> :abandon
no proof is being worked on
