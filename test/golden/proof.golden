thena spine> :theorem id : ∀ (A : Type₀) -> A -> A
proving id : ∀ (A : Type₀) -> A -> A
thena spine> :show
▶ let ? id : ∀ (A : Type₀) -> A -> A in
  id
thena spine> attack
thena spine> intro
thena spine> intro
thena spine> :show
▶ let ? id : ∀ (A : Type₀) -> A -> A ≐ (
    λ (A : Type₀) ->
    λ (_ : A) ->
    let ? id1 : A in
    id1
  ) in
  id
thena spine> into
thena spine> along
thena spine> along
thena spine> :where
focus
  let ? id1 : A in
path
  root ▸ ≐ id ▸ A ▸ _
context
  A : Type₀
  _ : A
type
  A
thena spine> try _
thena spine> solve
thena spine> back
thena spine> back
thena spine> back
thena spine> solve
thena spine> :show
▶ let id = λ (A : Type₀) (_ : A) -> let id1 = _ : A in id1 : ∀ (A : Type₀) -> A -> A in
  id
thena spine> qed
id : ∀ (A : Type₀) -> A -> A   ∎
thena spine> :show id
id : ∀ (A : Type₀) -> A -> A
id = let id = λ (A : Type₀) (_ : A) -> let id1 = _ : A in id1 : ∀ (A : Type₀) -> A -> A in id
thena spine> :proofs
no proofs
thena spine> :quit
